# QubitvaleTrading — Phase 0 dependencies
# RSS parsing uses the Python standard library (no feedparser needed).
ccxt>=4.5
pandas>=2.2
pyarrow>=15
duckdb>=1.0
requests>=2.31
PyYAML>=6.0
pytest>=8.0
# ETF-flow fallback: Farside gates on TLS fingerprint (not a JS challenge), so we
# fetch it via curl_cffi impersonating Firefox. Optional at runtime — the collector
# imports it lazily and degrades gracefully if absent.
curl_cffi>=0.7
# ---- Cross-asset DL research program (plan §14) ----
# torch in .venv: CPU build (--index-url https://download.pytorch.org/whl/cpu) — frozen
# families depend on CPU determinism. GPU work lives in the separate venv-gpu (cu130,
# GB10): see README "Two Python environments".
torch>=2.6
scikit-learn>=1.5
statsmodels>=0.14
exchange_calendars>=4.5
